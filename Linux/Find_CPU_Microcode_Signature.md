# Trouver la signature du microcode du processeur

```
iucode_tool --scan-system -v
```

Il est ensuite possible d'exécuter la commande `sudo apt changelog intel-microcode` pour vérifier si un microcode est disponible dans le paquet.
